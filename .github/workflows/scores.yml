name: Update live scores

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes (GitHub's scheduler is best-effort and may delay)
  workflow_dispatch: {}        # lets you run it manually from the Actions tab

permissions:
  contents: write

concurrency:
  group: live-scores
  cancel-in-progress: false

jobs:
  scores:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: claude/world-cup-betting-strategy-tsvivl

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Fetch scores
        env:
          FOOTBALL_DATA_TOKEN: ${{ secrets.FOOTBALL_DATA_TOKEN }}
        run: node scripts/fetch-scores.mjs

      - name: AI autopilot (settle + auto-pick next bet)
        run: node scripts/ai-autopilot.mjs

      - name: Commit if changed
        run: |
          git config user.name "scores-bot"
          git config user.email "actions@github.com"
          if ! git diff --quiet -- live.json ai-track.json; then
            git add live.json ai-track.json
            git commit -m "chore: update scores + AI autopilot [skip ci]"
            git push
          else
            echo "No changes to commit."
          fi
